global:
  resolve_timeout: 5m

route:
  receiver: "default"
  group_by: ["alertname", "job", "instance"]
  group_wait: 10s
  group_interval: 1m
  repeat_interval: 4h

receivers:
  - name: "default"
    # Local test: just keep alerts visible in the Alertmanager UI.
    # We'll add email/Slack/Teams later.
